{% set containerClass = 'page-about' %}
{% extends "base.twig" %}

{% block content %}

<div class="page-hero">
	<h1 class="page-title anim">{{ post.post_title }}</h1>
	<span class="arrow">{% include '/dist/img/right-arrow.svg.twig' %}</span>
</div>
<section class="about-bio">
	<div class="about-bio-image anim">
		<img src="{{ post.thumbnail.src }}" alt="">
	</div>
	<div class="about-bio-wrapper anim">
		<div class="about-bio-item">
			<h3 class="about-bio-title">Fondateurs / Représentants</h3>
			<ul>
				{% for author in authors %}
				<li>{{ author.post_title }}</li>
				{% endfor %}
			</ul>
		</div>
		<div class="about-bio-item">
			<h3 class="about-bio-title">Localisation</h3>
			<ul>
				<li>Koganei, Japon</li>
				<li><a href="">Voir la carte</a></li>
			</ul>
		</div>
		<div class="about-bio-item">
			<h3 class="about-bio-title">Création</h3>
			<ul>
				<li>15 Juin 1985</li>
			</ul>
		</div>
	</div>
</section>

<section class="quote anim">
	{% include '/dist/img/quote.svg.twig' %}
	<p class="quote-text">
		Je crois au pouvoir de l'histoire. Je crois que les histoires ont un rôle important à jouer dans la formation des êtres humains, qu'elles peuvent stimuler, étonner et inspirer leurs auditeurs
	</p>
	<span class="quote-author">Hayao Miyazaki</span>
</section>

	<section class="topic {{ sections[0].class }}">
		<h2 class="topic-title anim">{{ sections[0].post_title }}</h2>
		<p class="topic-paragraph anim">
            {{ sections[0].post_content }}
		</p>
        {% include './includes/about_media/media_'~sections[0].template~'.twig' %}
	</section>
	<section class="topic {{ sections[1].class }}">
		<h2 class="topic-title anim">{{ sections[1].post_title }}</h2>
		<p class="topic-paragraph anim">
            {{ sections[1].post_content }}
		</p>
        {% include './includes/about_media/media_'~sections[1].template~'.twig' %}
	</section>
	<section class="topic {{ sections[2].class }}">
		<h2 class="topic-title anim">{{ sections[2].post_title }}</h2>
		<p class="topic-paragraph anim">
            {{ sections[2].post_content }}
		</p>

        {% include './includes/about_media/media_'~sections[2].template~'.twig' %}
	</section>
	<section class="topic {{ sections[3].class }}">
		<h2 class="topic-title anim">{{ sections[3].post_title }}</h2>
		<p class="topic-paragraph anim">
            {{ sections[3].post_content }}
		</p>

        {% include './includes/about_media/media_'~sections[3].template~'.twig' %}
	</section>
{% endblock %}
